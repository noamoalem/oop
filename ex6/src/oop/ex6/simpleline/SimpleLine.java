package oop.ex6.simpleline;

import oop.ex6.Line;

import java.lang.reflect.Array;
import java.util.ArrayList;

/**
 * An interface for all the SimpleLine objects (not if/while or method)
 * that force them to implements the valid function that raise error if this line is illegal, extends Line interface.
 */
public interface SimpleLine extends Line {

}
